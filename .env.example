# Root environment example for local orchestration.
# Backend reads backend/.env directly; web reads web/.env.local.

APP_ENV=production

# Backend
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000

# Frontend
WEB_HOST=127.0.0.1
WEB_PORT=3000
